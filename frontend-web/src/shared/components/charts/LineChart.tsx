export default function LineChart() {
  return (
    <div className="h-64 flex items-center justify-center text-gray-400">
      Line Chart - integrate with chart library (recharts, chart.js, etc)
    </div>
  );
}
